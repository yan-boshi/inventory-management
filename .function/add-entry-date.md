# 各功能增加录入日期属性
## 销售订单增加录入日期属性
### 前端显示
 - 新增销售订单界面的发货日期改为录入日期，匹配销售订单表的录入日期属性，sales_items属性中新增发货日期属性，显示在备注前
 - 检索销售订单界面的发货日期改为录入日期，匹配销售订单表的录入日期属性
 - 打印销售订单界面新增发货日期列，在备注前，甲方和乙方下面的日期匹配录入日期
## 采购订单增加录入日期属性
### 前端显示
 - 检索采购订单界面的创建日期改为录入日期，匹配采购订单表的录入日期属性
 - 新增采购订单界面显示在采购合同编号下方，默认为当前年月日，可修改
 - 删除采购订单表的发货日期和到货日期属性，purchase_items中新增发货日期属性
 - 打印采购订单中的交期改为发货日期，匹配purchase_items中发货日期属性，订单日期匹配采购订单表的录入日期
## 报价单增加录入日期属性
### 前端显示
 - 检索报价单界面报价日期改为录入日期
 - 新增报价单界面显示在报价编号下方，默认为当前年月日，可修改
## 入库单增加录入日期属性
### 前端显示
 - 检索入库单界面新增录入日期列在总计后方
 - 新增入库单界面显示在采购订单编号下方，默认为当前年月日，可修改
## 出库单增加录入日期属性
### 前端显示
 - 检索出库单界面新增录入日期列在总计后方
 - 新增出库单界面显示在客户名称下方，默认为当前年月日，可修改

# 为我生成一份可以适配以上所有修改的sql文，方便我修改数据库