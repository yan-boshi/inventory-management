# 出库单转应付账款
在生成出库单的同时，自动创建应付账款记录。应付账款的source_bill_type默认为1，received_amount根据关联的销售合同编号的结算方式自动计算，目前只有预付100%和TT两种。due_date根据关联的销售合同编号的结算方式自动填写，TT则为生成出库单的出库时间，预付100%则为关联的销售合同编号的录入时间， status默认为0，未核销。用户可以在应付账款列表中查看和管理核销。