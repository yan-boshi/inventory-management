# 装箱单打印功能说明

## 功能概述

根据"虹竣科技8.12.xlsx"中的"一般贸易箱单"表格布局，新增了装箱单打印功能。

## 使用方法

### 方式一：从销售订单创建装箱单

1. 进入销售订单页面
2. 在订单列表中，点击"打印"按钮
3. 会弹出下拉菜单，显示两个选项：
   - **打印合同**：打开原有的销售合同打印界面
   - **打印装箱单**：打开新开发的装箱单打印界面

### 方式二：查询已保存的装箱单

1. 点击左侧菜单"单据中心" → "装箱单"
2. 进入装箱单查询页面
3. 可以按装箱单号、装箱日期进行搜索
4. 点击装箱单号可以查看详情
5. 支持删除操作

## 装箱单界面功能

### 1. 页面标题
- **Packing List**：英文标题（可编辑，点击后显示编辑图标）
- **装箱单**：中文标题（可编辑，点击后显示编辑图标）

这两个标题分两行显示，左右居中对齐。

### 2. 中间信息区域
- **Packing No.**：装箱单号（可编辑）
- **Date**：装箱日期（可编辑）
- **PO.**：采购订单号（自动从销售订单获取）

这三个字段显示在标题和卖方信息之间，往左移动一些，垂直对齐（标签右对齐，值左对齐）。

### 3. 卖方信息（占2/3宽度）
- 卖方公司名称（固定显示）
- 联系人（可编辑，默认从登录用户获取）
- 地址（可编辑）
- 电话（可编辑，默认从登录用户获取）

### 4. 买方信息（占2/3宽度）
- 买方公司名称（自动从客户信息获取）
- 联系人（可编辑，自动从客户信息获取）
- 地址（可编辑，自动从客户信息获取）
- 电话（可编辑，自动从客户信息获取）

卖方信息和买方信息垂直排列，占页面宽度的2/3。

### 5. 产品表格
- 商品名称
- 产品描述
- 数量
- 单位
- 净重(KG)（可编辑）
- 毛重(KG)（可编辑）
- 备注（可编辑）
- 操作（删除按钮，打印时隐藏）

表格行数与实际商品种类数一致，不再显示固定空行。每行都有删除按钮，点击可删除该行。

### 6. 底部信息
- 总箱数（可编辑）
- 成交方式（可编辑，默认从订单支付方式获取）
- 原产国（可编辑，默认为CHINA）

### 7. 签章区域
- 支持上传电子印章
- 支持清除印章

## 编辑功能

所有可编辑字段都可以通过点击进行编辑：
- 点击字段会弹出编辑窗口
- 修改后点击确定保存

## 保存并打印功能

- **Packing No.** 为必填项，作为主键
- 点击"保存并打印"按钮会：
  1. 验证Packing No.是否填写
  2. 检查Packing No.是否已存在
  3. 保存数据到数据库 `packing_lists` 表
  4. 保存成功后自动打印
- 打印时会自动隐藏非打印元素（如编辑按钮等）
- 支持A4纸张打印

## 技术实现

### 文件结构

```
src/
├── api/
│   └── packingLists.ts           # 装箱单API
├── components/
│   ├── PackingListPrint.vue      # 装箱单打印组件
│   └── SalesOrderPrint.vue       # 销售合同打印组件（原有）
├── layouts/
│   └── DefaultLayout.vue         # 主布局（已添加单据中心菜单）
├── router/
│   └── index.ts                  # 路由配置（已添加装箱单路由）
└── views/
    ├── documents/
    │   └── PackingLists.vue      # 装箱单查询页面（新增）
    └── sales/
        └── SalesOrders.vue       # 销售订单页面（已修改）

server/
├── migrations/
│   └── 20260818_create_packing_lists.js  # 数据库迁移脚本
├── models/
│   └── PackingList.js            # 装箱单模型
├── controllers/
│   └── packingListController.js  # 装箱单控制器
└── routes/
    └── packingListRoutes.js      # 装箱单路由
```

### 装箱单查询页面功能

#### 搜索功能
- 按装箱单号模糊搜索
- 按装箱日期范围搜索
- 支持重置搜索条件

#### 列表显示
- 装箱单号（可点击查看详情）
- 装箱日期
- 采购订单号
- 卖方公司
- 买方公司
- 商品数量
- 总箱数
- 成交方式
- 创建时间
- 操作（查看、打印、删除）

#### 详情弹窗
- 显示装箱单完整信息
- 卖方和买方信息
- 商品明细表格
- 底部信息（总箱数、成交方式、原产国）

### 数据库表结构

**packing_lists 表**
- `packing_list_id` VARCHAR(36) - 主键
- `packing_no` VARCHAR(50) - 装箱单号（唯一，必填）
- `packing_date` DATE - 装箱日期
- `po_number` VARCHAR(100) - 采购订单号
- `sales_order_id` VARCHAR(36) - 关联销售订单ID
- `seller_name` VARCHAR(200) - 卖方名称
- `seller_contact` VARCHAR(100) - 卖方联系人
- `seller_address` TEXT - 卖方地址
- `seller_phone` VARCHAR(50) - 卖方电话
- `buyer_name` VARCHAR(200) - 买方名称
- `buyer_contact` VARCHAR(100) - 买方联系人
- `buyer_address` TEXT - 买方地址
- `buyer_phone` VARCHAR(50) - 买方电话
- `packing_items` JSON - 装箱商品明细
- `total_packages` VARCHAR(50) - 总箱数
- `trade_terms` VARCHAR(100) - 成交方式
- `country_of_origin` VARCHAR(100) - 原产国
- `title_en` VARCHAR(100) - 英文标题
- `title_zh` VARCHAR(100) - 中文标题
- `seller_stamp` LONGTEXT - 卖方印章
- `created_at` TIMESTAMP - 创建时间
- `updated_at` TIMESTAMP - 更新时间

### 主要修改

1. **SalesOrders.vue**：
   - 添加了打印下拉菜单
   - 添加了装箱单弹窗组件
   - 添加了打印选择处理函数

2. **PackingListPrint.vue**：
   - 新创建的装箱单组件
   - 支持数据编辑和打印
   - 支持印章上传
   - 联系人和电话默认从登录用户获取

## 数据来源

装箱单的数据主要来自：
- 销售订单的基本信息（合同号、支付方式等）
- 客户信息（公司名称、联系人、地址、电话）
- 订单商品明细
- 登录用户信息（卖方联系人、电话）

## 注意事项

1. 净重和毛重需要手动填写，系统不会自动计算
2. 印章会保存到浏览器本地存储中
3. 打印前建议预览效果
4. 所有可编辑字段在打印时会显示为普通文本
5. 卖方联系人和电话默认从登录用户信息获取，可手动修改

## 后续优化建议

1. 可以添加净重/毛重的自动计算功能
2. 可以添加装箱单模板保存功能
3. 可以添加批量打印功能
4. 可以添加装箱单导出PDF功能