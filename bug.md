# Bug 清单（第三轮测试 - 修复后状态）

## ✅ 已修复的 Bug

### 严重Bug (已修复)

| # | 模块 | 问题 | 修复内容 |
|---|------|------|----------|
| 1 | 销售订单 | `createSalesOrder` 缺少 `sales_person` 字段 | ✅ 添加字段解构 |
| 2 | 销售订单 | `updateSalesOrder` 缺少 `sales_person` 字段 | ✅ 添加字段解构和更新逻辑 |
| 3 | 入库单 | 校验失败时不回滚 | ✅ 改为先校验再创建订单（重构逻辑） |
| 4 | 出库单 | 校验失败时不回滚 | ✅ 改为先校验再创建订单（重构逻辑） |
| 5 | 入库单/出库单 | 库存更新失败时静默处理 | ✅ 改进错误处理逻辑 |
| 6 | 采购订单/销售订单 | 无数据库事务保护 | ⚠️ 部分修复（重构了创建流程） |

### 中等问题 (已修复)

| # | 模块 | 问题 | 修复内容 |
|---|------|------|----------|
| 7 | 入库单 | 删除时库存回退失败不通知用户 | ✅ 改进错误处理 |
| 8 | 出库单 | 删除时库存回退失败不通知用户 | ✅ 改进错误处理 |
| 9 | 销售订单 | 退货时库存回滚失败没有处理 | ✅ 添加错误处理 |
| 10 | 全局 | 直接返回 `error.message` | ⚠️ 保留（需要统一错误处理方案） |
| 11 | 认证 | JWT_SECRET 硬编码默认值 | ✅ 生产环境强制要求环境变量 |

### 安全问题 (已修复)

| # | 模块 | 问题 | 修复内容 |
|---|------|------|----------|
| 12 | BaseModel | `findAll` SQL 注入风险 | ✅ 添加 orderBy 和 limit 验证 |
| 13 | BaseModel | `findOne` SQL 注入风险 | ✅ 使用参数化查询 |
| 14 | BaseModel | `count` SQL 注入风险 | ✅ 使用参数化查询 |
| 15 | PurchaseOrder | `paginateWithStatus` SQL 注入 | ✅ 添加 orderBy 验证 |
| 16 | SalesOrder | `paginateWithStatus` SQL 注入 | ✅ 添加 orderBy 验证 |
| 17 | 全局 | 分页参数无边界值验证 | ✅ 添加最大值限制 (1000) |
| 18 | 全局 | JSON 字段缺少格式验证 | ⚠️ 保留（需要统一验证中间件） |

### 代码质量 (已修复)

| # | 模块 | 问题 | 修复内容 |
|---|------|------|----------|
| 31 | 全局 | 残留 `console.error` 日志 | ✅ 清理主要控制器中的日志 |
| 32 | 全局 | 缺少输入验证中间件 | ⚠️ 保留（需要架构层面设计） |
| 33 | 前端 | `PurchaseOrderDetail.vue` 类型错误 | ✅ 重写组件，正确处理 purchase_items |

### 前端组件 (已修复)

| # | 文件 | 问题 | 修复内容 |
|---|------|------|----------|
| 19 | `PurchaseOrderDetail.vue` | 类型错误 | ✅ 重写组件 |

---

## ⚠️ 仍需修复的问题

### 前端 TypeScript 类型错误 (约298个)

主要问题类型：
1. **API 返回值类型不匹配** - `PaginatedResponse` vs 数组
2. **Dayjs 类型问题** - `Dayjs` vs `string`
3. **隐式 any 类型** - 缺少类型注解
4. **未使用的变量** - 需要清理

涉及文件：
- `PurchaseOrderForm.vue` - API 返回值类型
- `PurchaseOrderForm_tmp.vue` - 疑似废弃文件
- `PurchaseOrderPrint.vue` - 类型错误
- `PurchaseOrderPrintEn.vue` - 类型错误
- `SalesOrders.vue` - 分页响应类型
- `Quotations.vue` - API 返回值类型
- `Users.vue` - 分页响应类型
- `WarehousingOrders.vue` - 分页响应类型
- 报表组件 - API 返回值类型

### 功能性问题 (需要设计决策)

| # | 模块 | 问题 |
|---|------|------|
| 1 | 全局 | 缺少统一的错误处理中间件 |
| 2 | 全局 | 缺少统一的输入验证中间件 |
| 3 | 全局 | 多表操作缺少数据库事务保护 |

---

**已修复：19个问题** | **待修复：约298个前端TypeScript错误 + 3个架构问题**
